#include <stdio.h>

/**
*Main - Entry point
*return: always 0 (success)
*/
int main(void)
{
int n;
{
    /* prints all single digit numbers of base 10 starting from 0 */
}
for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");
return (0);
}
