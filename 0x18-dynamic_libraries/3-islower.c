#include "main.h"

/**
 * _islower - checks for a lowercase character
 * @c: The character to check
 * Return: 1 if c is lowercase, Nothing otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

